# 국민대 약술 모의고사 검수 시스템

조교가 모의고사를 풀고 답을 옮겨 적은 뒤, 같은 기준으로 문항을 검수하는 도구. 종이로 풀고 결과만 입력하는 OMR 방식이다.

## 폴더

```
kookmin-review/
  index.html            셸, PIN 게이트, 화면 전체 스타일
  js/config.js          SCRIPT_URL, 조교 명단  ← 여기만 고치면 운영 설정이 바뀐다
  js/checklist.js       검수 항목 6묶음 29개
  js/precheck.js        자동 사전점검 엔진
  js/store.js           저장소 (로컬 또는 구글시트)
  js/app.js             탭 라우터
  js/views/             submit 답안제출, grade 채점, review 검수, dash 대시보드
  data/build_round.py   HWPX 시험지를 회차 JSON 으로 바꾸는 변환기
  data/push_round.py    회차 JSON 을 시트에 올리는 스크립트
  data/rounds.js        서버 없이 로컬로 볼 때만 쓰는 회차 파일 (저장소에 올리지 않는다)
  apps-script/Code.gs   구글시트 백엔드
```

로컬 미리보기: `.claude/launch.json` 의 `kookmin-review` (http://127.0.0.1:8799)

## 화면 네 개

1. **답안 제출** — 문항별 소문항 발문과 배점, 조건을 띄우고 답 입력칸만 준다. 입력하는 대로 저장되고, 제출을 눌러야 채점 화면이 열린다.
2. **채점** — 찾기형은 모범답안과 글자가 같은지 자동으로 맞춰 O 또는 X 를 미리 눌러 둔다. 최종 판정은 조교가 O, △, X 로 누른다. 환산 점수가 실시간으로 뜬다.
3. **검수** — 문항을 고르면 그 문항의 자동 사전점검 결과가 먼저 뜨고, 아래에 29개 항목이 여섯 묶음으로 나온다. 항목마다 이상 없음(○), 문제 있음(✕), 해당 없음(－). 묶음별로 한 번에 통과시키는 버튼이 있다. 문제 있음을 고르면 메모칸이 열리고, 자동 점검 문구가 초안으로 들어간다.
4. **대시보드** — 가경T만 보인다. 소문항별 정답률과 조교들이 쓴 오답 원문, 검수 지적 목록, 자동 점검 전체, 조교별 진행. 검수 보고서 CSV 와 답안 취합 CSV 로 내보낸다.

## 검수 항목 29개

출처는 두 가지를 합쳤다.
- 국민대 약술 논술 검수 가이드라인(외부 검수진 배부용) 3-1 ~ 3-5
- 조교 실무 체크리스트 10항목

| 묶음 | 내용 | 개수 |
|---|---|---|
| A | 원문 대조 — 원문 일치, 발췌 규칙, 출처 표기 | 3 |
| B | 정답 근거 — 지문 내 실재, 발췌 범위, 문구 일치, 단위 조건, 갈래복합 | 5 |
| C | 복수 정답 가능성 — 동의어, 상위어와 예시, 조건의 유일성, 어미 호응, 최상급, 1:1 대응 | 6 |
| D | 발문, 정답, 해설, 채점기준 정합성 — 오류가 가장 많이 나는 묶음 | 8 |
| E | 배점 — 합 10.0, 채점기준 배점 일치, 부분점수 일관성 | 3 |
| F | 표기 — 따옴표, 가운뎃점, 오탈자, 기호 순서 | 4 |

★ 표시가 붙은 항목은 가이드라인이 특별히 짚은 것이다. 항목 문구를 고치려면 `js/checklist.js` 만 건드리면 된다.

## 자동 사전점검

회차 데이터에 값이 있을 때만 돌고, 없으면 조용히 건너뛴다. 조교가 눈으로 세지 않아도 되는 것들을 대신 센다.

| 항목 | 무엇을 잡나 |
|---|---|
| A1 | 지문이 원문과 어긋나는 지점 (회차 데이터에 `origin` 을 넣었을 때만) |
| A2 | 중략이 2회 이상이거나 (중략) 외의 생략 표기 |
| B2, B3 | 찾기형 정답이 시험지 발췌분 안에 문구 그대로 있는지. 여러 빈칸 정답은 조각마다 따로 본다 |
| B4 | 어절, 음절, 한 단어 조건과 정답이 어긋나는 경우. 각각이 붙으면 조각마다 |
| C1 | 정답 어휘가 지문에 두 번 이상 나오는 경우 |
| C2 | 정답이 상위어의 괄호 예시 목록 안에 들어 있는 경우 |
| D1 | 서술하시오 계열 동사, 찾아 쓰시오에 서술 조건이 붙은 경우 |
| D2, D3 | N자 내외와 모범답안 길이, 한 문장 조건과 문장 수 |
| D4 | 한 문항의 정답이 다른 소문항 정답에 그대로 들어 있는 경우 |
| E1 | 문항 배점 합이 10.0이 아닌 경우 |
| E2 | 채점기준 부분점수의 합이 발문 배점과 다른 경우 |
| F1 | 곧은따옴표 잔존 |
| F2 | 가운뎃점. 지문은 원문이므로 대상에서 뺀다 |
| F4 | 기호(㉠㉡, ⓐⓑ)가 등장 순서와 어긋나는 경우 |

E2 는 채점기준이 글로만 적혀 있으면 문장에서 점수를 읽어 내 추정하므로 경고로만 올린다. 표 형태로 `rubric: [{text, score}]` 를 넣어 두면 확정 판정으로 바뀐다.

## 새 회차 넣기

```bash
python data/build_round.py                      # HWPX -> data/round-<id>.json
python data/push_round.py round-final-03.json 7452   # JSON -> 시트
```

`build_round.py` 아래쪽 `src` 를 그 회차 HWPX 경로로, `rid` 와 `title` 을 그 회차 이름으로 바꾸고 돌린다. 올린 회차는 상단 드롭다운에 함께 뜬다.

파서가 기대하는 HWPX 구조:
- `인문계열` … `문제 N` … `(1) 발문 (2.4점)` … `자연계열` … `논술고사해설지` …
- 해설지 블록은 `정답 및 채점 기준` → `정답` → `(1)` → 내용 → `해설` → `채점 기준` 순
- 해설지 블록이 시험지 문항과 같은 순서로 있어야 짝이 맞는다

## 배포와 백엔드

- **라이브**: https://oreum1222.github.io/kmreview/ (저장소 `oreum1222/kmreview`, 공개)
- **시트**: https://docs.google.com/spreadsheets/d/1D58iLpusHQ67TVWJiSa64AjDdmPKOACOV-VDZh_LMP4/edit
- **스크립트**: https://script.google.com/d/1u63sVJfCY1dBNINV1RAfCi393mY8uVP7RlBxU-tOl2QdL6wLnOfkqYTo/edit
- **배포 ID**: `AKfycbyZ4nflFQ97yyXxcmtJVmYRY7AENC8ixy3BWMUtbW_G5i0V6_uYptxh0y26Mk1b_om0`
- **clasp 폴더**: `C:\ogwork\gas-kmreview` (한글 경로에서 clasp 가 깨지므로 ASCII 경로에 둔다)
- 백엔드 갱신은 `clasp push` 후 `clasp update-deployment <배포ID>` 로 버전만 올려 주소를 유지한다.

**시험지 내용은 저장소에 없다.** `.gitignore` 가 `data/rounds.js` 와 `data/*.json` 을 막고, 회차는 시트의 `rounds` 탭에서 PIN 인증을 통과한 사람에게만 내려간다. 시트 셀은 5만 자가 한계라 회차 JSON 을 4만 자씩 조각내 여러 행에 담고 읽을 때 다시 잇는다.

**PIN 도 저장소에 없다.** 입력한 PIN 을 서버로 보내 Apps Script 속성 `REVIEW_PIN` 과 맞춰 본다. `js/config.js` 의 `PIN` 은 서버 없이 로컬로 돌릴 때만 쓰인다.

## 운영 설정

- **PIN** — `js/config.js` 의 `PIN`. 구글시트 백엔드를 붙이면 Apps Script 속성 `REVIEW_PIN` 도 같은 값으로.
- **조교 명단** — `js/config.js` 의 `STAFF`. 드롭다운에서만 고르게 해서 이름 표기가 흔들리지 않는다.
- **대시보드 권한** — `js/config.js` 의 `ADMIN`.
- **PIN 바꾸기** — 스크립트 편집기 프로젝트 설정에서 스크립트 속성 `REVIEW_PIN` 을 고친다. 프런트는 손댈 것이 없다.
- **회차 올리기** — `python data/push_round.py round-final-03.json <PIN>`. 같은 id 면 덮어쓴다.

## 알아 둘 것

- PIN 을 통과하면 모범답안과 해설, 채점기준이 브라우저로 내려온다. 검수진은 해설지를 이미 받아 보는 사람들이라 그대로 두었다. 답안 제출 전 정답 노출까지 막으려면 정답만 따로 늦게 내려보내야 한다.
- 답안을 내지 않은 채 검수 탭에 들어가면 정답이 보인다고 한 번 알리고 확인을 받는다.
- `file://` 로 열면 구글시트 연결이 CORS 로 막힌다. 로컬 확인은 launch.json 의 로컬 서버로 한다.
- 첫 로드는 Apps Script 콜드 스타트로 10초 넘게 걸릴 수 있다.
- `js/config.js` 의 `STAFF` 에 조교 이름이 들어 있고 공개 저장소에서 보인다. 연락처는 없다. 이름도 가려야 하면 명단을 시트로 옮겨야 한다.

## 3회 기준 첫 점검 결과

`2027_CSM_국민대_파이널_3회_0908 가경T 수정완.hwpx` 로 만들어 돌린 결과, 자동 점검에 16건이 걸렸고 그중 8건이 반드시 볼 것이다. 배점 합은 10문항 모두 10.0으로 맞다. 눈에 띄는 것:

- **문제 1 (1)** 발문은 〈보기〉의 ㉠에 들어갈 2음절 말을 찾으라는 것인데, 정답과 해설과 채점기준은 지문의 ㉠(효조라는 전통적 평가)에 대한 옛 판본 그대로다. 게다가 지문의 ㉠과 〈보기〉의 ㉠이 같은 기호로 겹친다.
- **문제 4 (1)** 발문은 `~과 ~` 꼴로 각각 2음절을 요구하는데(충전과 방전으로 보인다), 정답과 해설은 자연계열 판본의 FR-ESS 그대로다.
- **문제 7 (2)** 발문은 찾아 쓰시오인데 정답이 지문에 그 문구로 없고 채점기준은 부분점수를 나눈 서술형이다.
- **문제 8 (1)** 채점기준 1.5 + 1.5 = 3.0 인데 배점은 4.0. **(2)** 는 1.5 + 1.5 = 3.0 인데 배점은 6.0.
- 문제 2 (2), 4 (2), 6 (2), 9 (2) 는 모범답안이 N자 내외 조건을 20% 넘게 초과한다.
